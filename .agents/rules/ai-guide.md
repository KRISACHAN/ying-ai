# AI Guide

Follow [docs/ai/core/](../../docs/ai/core/) for universal rules (principles, verification, git protocol, project context).

Read [docs/ai/platforms.md](../../docs/ai/platforms.md) for the cross-tool adapter map. Canonical project rules live in `.agents/rules/`; tool adapters: `.codex/rules/`, `.cursor/rules/`, `.claude/rules/`.

Project wizard: [AGENTS.md](../../AGENTS.md) (single entry for all tools). [CLAUDE.md](../../CLAUDE.md) and [GEMINI.md](../../GEMINI.md) redirect here.

When editing files under `apps/` or `packages/`, also read the nearest nested `README.md` in that package (e.g. `apps/model-runtime-demo/README.md`).
