import { ModelRuntimePanel } from "../../model-runtime-panel";

export default function ModelRuntimeDebugPage() {
  return (
    <main className="shell">
      <ModelRuntimePanel />
    </main>
  );
}
